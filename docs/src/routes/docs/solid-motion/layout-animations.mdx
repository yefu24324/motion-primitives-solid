import { ComponentPreview } from "@/components/website/component-preview";
import { BasicLayoutFlexSwitchDemo } from "@/examples/solid-motion/basic-layout-flex-switch-demo";
import { BasicLayoutTabsDemo } from "@/examples/solid-motion/basic-layout-tabs-demo";
import { ReorderAnimationsDemo } from "@/examples/solid-motion/reorder-animations-demo";

# Layout animation

Motion's industry-leading layout animations makes it easy to animate between any two layouts, even between different elements.

It's as simple as a 'layout' prop.

```tsx
<motion.div layout />
```

This little prop can animate previously unanimatable CSS values, like switching justify-content between 'flex-start' and 'flex-end'.

```tsx
animateView(() => {
  setIsOn(!isOn);
});

return (
  <Motion
    layout
    style={{ justify-content: isOn() ? 'flex-start' : 'flex-end' }}
  />
);
```

<ComponentPreview component={BasicLayoutFlexSwitchDemo}>
```tsx file=<rootDir>/docs/src/examples/solid-motion/basic-layout-flex-switch-demo.tsx

````
</ComponentPreview>

## Layout Tabs

<ComponentPreview component={BasicLayoutTabsDemo}>
```tsx file=<rootDir>/docs/src/examples/solid-motion/basic-layout-tabs-demo.tsx

````

</ComponentPreview>

## Reorder Animations

<ComponentPreview component={ReorderAnimationsDemo}>
```tsx file=<rootDir>/docs/src/examples/solid-motion/reorder-animations-demo.tsx

```

</ComponentPreview>
```
