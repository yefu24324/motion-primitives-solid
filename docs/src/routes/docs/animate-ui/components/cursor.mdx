import { AnimationUICursor } from '@/examples/animate-ui/components/cursor-demo';
import { CursorFollowOnlyDemo } from '@/examples/animate-ui/components/cursor-follow-only-demo';
import { ComponentPreview } from '@/components/website/component-preview';

# Cursor

An animated cursor component that allows you to customize both the cursor and cursor follow elements with smooth animations.

<ComponentPreview component={AnimationUICursor}>
```tsx file=<rootDir>/docs/src/examples/animate-ui/components/cursor-demo.tsx

```
</ComponentPreview>


## Examples

### Cursor Follow Only

You can use only the `CursorFollow` (like the example below) component or the `Cursor` component.

<ComponentPreview component={CursorFollowOnlyDemo}>
```tsx file=<rootDir>/docs/src/examples/animate-ui/components/cursor-follow-only-demo.tsx

```
</ComponentPreview>